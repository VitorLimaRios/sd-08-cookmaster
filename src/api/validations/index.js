const valid = require('./valid');
const validEmail = require('./validEmail');
const validLogin = require('./validLogin');
const validPassword = require('./validPasswod');
const validRecipes = require('./validRecipes');

module.exports = {
  valid,
  validEmail,
  validLogin,
  validPassword,
  validRecipes,
};
