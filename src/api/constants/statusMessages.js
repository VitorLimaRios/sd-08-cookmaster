const INVALID_ENTRIES = 'Invalid entries. Try again.';
const ALREADY_REGISTERED = 'Email already registered';
const ALL_FIELDS_REQUIRED = 'All fields must be filled';
const INCORRECT_USER_OR_PASS = 'Incorrect username or password';
const JWT_MALFORMED = 'jwt malformed';


module.exports = {
  INVALID_ENTRIES,
  ALREADY_REGISTERED,
  ALL_FIELDS_REQUIRED,
  INCORRECT_USER_OR_PASS,
  JWT_MALFORMED,
};
